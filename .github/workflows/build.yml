name: Build Boston Overlay Fix
on: [workflow_dispatch]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Install Official Android SDK Platform 35
        run: |
          mkdir -p $HOME/Android/Sdk/cmdline-tools/latest
          wget -q https://google.com
          unzip -q commandlinetools-linux-11076708_latest.zip
          mv cmdline-tools/* $HOME/Android/Sdk/cmdline-tools/latest/
          export ANDROID_HOME=$HOME/Android/Sdk
          export PATH=$PATH:$ANDROID_HOME/cmdline-tools/latest/bin
          yes | sdkmanager --licenses > /dev/null
          sdkmanager "platforms;android-35" "build-tools;35.0.0"

      - name: Inject Framework Resource Table
        run: |
          # Automatically injects clean framework resource mapping for API 35
          mkdir -p $HOME/Android/Sdk/platforms/android-35/
          cp $HOME/Android/Sdk/platforms/android-35/android.jar ./framework-res.apk || true

      - name: Build Module
        run: |
          export ANDROID_HOME=$HOME/Android/Sdk
          export FRAMEWORK_APK=$HOME/Android/Sdk/platforms/android-35/android.jar
          chmod +x build.sh
          ./build.sh

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: boston_gsi_overlay_fix_v1
          path: boston_gsi_overlay_fix_v1.zip
