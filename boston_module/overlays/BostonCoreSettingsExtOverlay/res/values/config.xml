<?xml version="1.0" encoding="utf-8"?>
<!--
  BostonCoreSettingsExtOverlay - VERIFIED VALUES ONLY
  Source: overlay/product/MotoCoreSettingsExtBostonOverlay.apk, decoded
  directly. This was the entire content of that APK's resource table -
  one array, nothing else.
-->
<resources>

    <!--
      supported_refresh_rate_value
      VERBATIM array from stock Boston overlay: ["0", "60", "120"]
      List of refresh rates (Hz) exposed in Motorola's display settings
      UI. 0 likely represents an "Auto/Adaptive" option rather than a
      literal 0Hz mode. Matches the panel's confirmed 120/90/60Hz modes
      seen in the SurfaceFlinger dump, though note this array omits 90Hz
      entirely - preserved exactly as shipped, not corrected, since this
      is what Motorola's own settings UI presented on the stock ROM.
    -->
    <integer-array name="supported_refresh_rate_value">
        <item>0</item>
        <item>60</item>
        <item>120</item>
    </integer-array>

</resources>
